spring.application.name=gigwork

# Server Port
server.port=8080

# PostgreSQL Database Connection
spring.datasource.url=jdbc:postgresql://ep-super-poetry-a1zl1gh8-pooler.ap-southeast-1.aws.neon.tech/neondb
spring.datasource.username=neondb_owner
spring.datasource.password=npg_vKeO3p1EfItd

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# SQL Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Locations OpenAPI (행정안전부 법정동 코드) - DB-less proxy settings
# You may provide multiple URLs separated by commas; service will try each in order.
openapi.legal.url=https://apis.data.go.kr/1741000/StanReginCd/getStanReginCdList
# Put your Decoding Key here (server-side). Leave blank to test fallback-only SI list.
openapi.legal.key=
# Debug logging for LocationService
logging.level.com.example.gigwork.service.LocationService=DEBUG
